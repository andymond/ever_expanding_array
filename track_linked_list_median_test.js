const chai = require('chai');
const should = chai.should();
const track_median = ('./track_linked_list_median.js')

describe('tracks linked list median', () => {
  it('it can find linked list median at a certain node', () => {

  })

  it('it generates a linked list of random values', () => {

  })

  it('it generates a linked list of medians of those random values', () => {

  })
})
